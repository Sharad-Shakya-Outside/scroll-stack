import { scrollCards } from "./scrollCards.js";
import { slidePanel } from "./slidePanel.js";

document.addEventListener("DOMContentLoaded", () => {
  slidePanel();
  scrollCards();
})

// window.addEventListener('scroll', () => {
// })
